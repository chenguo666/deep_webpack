import { dataFormat } from "./utils/format";
import _ from "lodash";
const message = "i am a message xxxxx";
console.log(message);
const sum = (num1, num2) => {
  return num1 + num2;
};
console.log(dataFormat());
console.log(_.join("asdf", "fsada"));
export { sum };
